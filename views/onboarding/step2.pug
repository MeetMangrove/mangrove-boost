extends ../layout

block content
  .row
    .col-md-6.col-md-offset-3
      .main-container
        .row.justify-content-md-center
          .col-md-6.col-md-offset-3
            ul.loader
              li.point.active
              li.point.active
              li.point
        h2 Awesome #{user.profile.firstName}! 👋
        br
        h5 Here you go 👇
        .row
          .col-md-12
            if !user.twitter 
              a.btn.btn-block.btn-twitter.btn-social.text-center(href='/auth/twitter')
                i.fa.fa-twitter
                | Connect with Twitter
              br
              small (No content will be tweeted without your approval)
            else
              | You are already connected your Twitter account 🏄🏻
