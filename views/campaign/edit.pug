extends ../layout

block content
  .row
    .col-md-6.col-md-offset-3
      .main-container
        .row.justify-content-md-center
          .col-md-12  
            form.form-horizontal(id='new-campaign-form', method='POST')
              input(type='hidden', name='_csrf', value=_csrf)
              .form-group
                label.col-sm-3.control-label(for='name') Name
                .col-sm-7
                  input.form-control(type='text', name='name', id='name', placeholder='Name of Campaign', autofocus, required)
              .form-group
                label.col-sm-3.control-label(for='link') Link
                .col-sm-7
                  input.form-control(type='url', name='link', id='link', placeholder='Link you want to share', required)
              .form-group
                label.col-sm-3.justify-content-md-center.control-label(for='content') Content
                .col-md-7
                  textarea.form-control(name='content', id='content', placeholder='Content you want to share', required)
              .form-group
                label.col-sm-3.control-label(for='date_release') Date
                .col-sm-7
                  input.form-control.date(type='datetime-local', name='date_release', id='date_release', placeholder='When do you want to spread the vibe', required)
                  span 
                    small
                      | Paris GMT+1
              br
              button.btn.primary-btn(type='submit') 
                h4 🔥 🔥 Boom 🔥 🔥
  script.
    $(function () {
        $('#date').datetimepicker();
    });
    console.log("coucou");
